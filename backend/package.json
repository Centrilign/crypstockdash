{
  "name": "crypstockdash-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live market data API for CrypStockDash. Stocks via Yahoo Finance (yahoo-finance2), crypto via CoinGecko. Both free, no API key required.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "yahoo-finance2": "^4.0.0"
  }
}
